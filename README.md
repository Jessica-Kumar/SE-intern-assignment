# SE-intern-assignment
Completed both problems — LRU Cache (O(1) get/put, thread-safe version included) and Event Scheduler (overlap detection + min-heap room allocation + named room assignment). All code tested with outputs. Full analysis in attached document.
